/** Stub — Import 畫面屬後續批次；BottomNav 第二格的目的地。 */

import { AppShell } from '@/components/app-shell'

export default function Page() {
  return (
    <AppShell>
      <p className="text-body-sm text-copy">匯入（尚未實作）</p>
    </AppShell>
  )
}
